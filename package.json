{
  "name": "whiteboard",
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/react": "^19.2.5",
    "@types/react-dom": "^19.2.3",
    "tsup": "^8.0.2",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "pnpm -r build",
    "bench": "pnpm --filter @whiteboard/engine run bench:check",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
