{
  "name": "@whiteboard/engine",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@whiteboard/core": "workspace:*"
  },
  "scripts": {
    "build": "tsup",
    "bench:drag-frame": "tsup src/perf/dragFrame.bench.ts --format esm --outDir .bench --clean --silent && node .bench/dragFrame.bench.js",
    "bench:drag-frame:check": "WB_BENCH_ENFORCE=1 pnpm run bench:drag-frame",
    "bench:node-hint": "tsup src/runtime/lifecycle/watchers/nodeHint.bench.ts --format esm --outDir .bench --clean --silent && node .bench/nodeHint.bench.js",
    "bench:node-hint:check": "WB_BENCH_ENFORCE=1 pnpm run bench:node-hint",
    "bench:check": "pnpm run bench:drag-frame:check && pnpm run bench:node-hint:check",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"No tests\""
  }
}
